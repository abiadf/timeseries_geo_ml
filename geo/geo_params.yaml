
dataset:
  general:
    epochs            : 50
    earlystop_patience: 5

  china_weather:
    base:
      {cyclic_threshold: 0.25, window_size: 8, sliding_size: 1, z_dim_total: 24, hidden_dim: 64, lr_optimizer: 1e-3, batch_size: 128}
    override:
      {window_size: 64, sliding_size: 16, z_dim_total: 56, hidden_dim: 36, lr_optimizer: 0.0004, batch_size: 256, epochs: 50,
      cyclic_threshold: 0.2700, earlystop_patience: 8, lambda_recon: 1.6108, lambda_latent: 0.0020, task: forecast}

  electric_power:
    cyclic_threshold: 0.5
    # window_size : 8
    # sliding_size: 1
    # z_dim_total : 24 #16
    # hidden_dim  : 64
    # lr_optimizer: 1e-3
    # batch_size  : 128

  longterm_weather:
    cyclic_threshold: 0.27
    # window_size : 8
    # sliding_size: 1
    # z_dim_total : 24 #16
    # hidden_dim  : 64
    # lr_optimizer: 1e-3
    # batch_size  : 128




