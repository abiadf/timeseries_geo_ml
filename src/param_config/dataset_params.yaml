# file for the params of DATASETS
general:
  train_epochs:      100
  num_pages_to_use:  10
  window_len:        128
  window_len_list:   [32, 64, 128, 256, 512, 1024, 2048]

asm:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset

  # train_epochs: 100
  batch_size        : 16
  latent_dim        : 32
  predictor_lr      : 0.006
  predictor_epochs  : 200
  predictor_dropout : 0.05
  predictor_hidden_sizes : [32, 48, 64] # latent to y output

beijing:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 512

  timevae:
    lr_training       : 0.001
    latent_dim        : 15

panama:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 32

china_weather:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 2048 #4096

  # train_epochs: 100
  batch_size        : 16
  latent_dim        : 6
  predictor_lr      : 0.006
  predictor_epochs  : 200
  predictor_dropout : 0.05
  predictor_hidden_sizes : [32, 48, 64] # latent to y output

gas:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 2048 #4096

germany_catchment:
  num_window_splits: 10 # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 512

  # train_epochs: 100
  batch_size        : 16
  latent_dim        : 8
  predictor_lr      : 0.006
  predictor_epochs  : 200
  predictor_dropout : 0.05
  predictor_hidden_sizes : [32, 48, 64] # latent to y output

india_catchment:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 512

  # train_epochs: 100
  batch_size        : 16
  latent_dim        : 8
  predictor_lr      : 0.006
  predictor_epochs  : 200
  predictor_dropout : 0.05
  predictor_hidden_sizes : [32, 48, 64] # latent to y output

nasa:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 300  # number of rows per page to use from dataset

panama:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 300  # number of rows per page to use from dataset
  window_len       : 32

weather:
  num_window_splits: 10   # splits timesteps (rows) into this many windows
  num_rows_per_page: 500  # number of rows per page to use from dataset
  window_len       : 2048

  # train_epochs: 100
  batch_size        : 16
  latent_dim        : 3
  predictor_lr      : 0.006
  predictor_epochs  : 200
  predictor_dropout : 0.05
  predictor_hidden_sizes : [32, 48, 64] # latent to y output
